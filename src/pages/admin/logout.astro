---
// admin/logout.astro - Clears session cookie
import { Astro } from 'astro/runtime';

const ADMIN_SESSION_COOKIE = 'admin_session_id';

Astro.cookies.delete(ADMIN_SESSION_COOKIE, { path: '/admin' }); // Clear the cookie

return Astro.redirect('/admin/login');
---